# packages/config

`tsconfig.base.json`, preset de ESLint (incluyendo la regla `no-restricted-imports` que bloquea `next/*` en `packages/core` y `packages/primitivas`) y configuración de Prettier compartidos por las 6 aplicaciones.
